import React, { Component, Fragment } from 'react';
import { questions } sampleData;



class LocalData extends Component {

  renderTitleList = () => {
    const list = titles.map( record => {
      return (
        <Text style={{fontSize: 30}}>{record.title}</Text>
      )
    })
    return list;
  }

  render(){
    return(
      <div>

      </div>
    );
  }
}

export default State;
